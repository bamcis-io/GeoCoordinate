namespace BAMCIS.GIS
{
    /// <summary>
    /// The distance type options for providing distance results
    /// when computing the length between two GeoCoordinates.
    /// </summary>
    public enum DistanceType
    {
        MILES,
        METERS,
        KILOMETERS
    }
}
